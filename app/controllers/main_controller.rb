class MainController <  ApplicationController
	def index
		#flash.now[:notice] = "Logged in successfully"
		#flash.now[:alert] = "Invalid email"

		#if session[:user_id]
			#@user = User.find_by(id: session[:user_id]) #find by no se cae si no lo encuentra 
		#end
	end
end